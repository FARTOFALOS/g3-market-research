# Станок

Изменяемое состояние проекта: что сейчас лежит на столе, чем это читать и куда
что класть. Правила работы и мандат — в [`AGENTS.md`](AGENTS.md); этот файл
устаревает быстрее и меняется без пересмотра тех правил.

## Что сейчас на столе

`as-of` 2026-09-24. Это указатель: числа и оговорки живут в карточках и журналах.

**Этап — наблюдение вперёд, поиск на паузе** (решение трейдера и соавтора 2026-09-24). Главная линия G3 —
библиотека независимых положительных выплат; единственный живой её член — **B1** («утро S-07, затем S-18» одним
контрактом, [`FREEZE_B1`](setups/library/FREEZE_B1.md)): исторически +$245 на сессию NQ 2020–2026-05, хвостовой,
слепой проверки нет. Следующее сильное свидетельство о нём — будущие сессии, а не новый разрез прошлого.
Свежую совместимую ленту не покупаем (Databento отложен, скрипты в `work/databento/`); появится бесплатно — B1
дописывает слепой журнал тем же движком.

**Новый поиск на минутной NQ цене и времени не открывается.** 2026-09-24 закрыты по объявлениям до счёта
([журнал 105](base/105/LOG.md)): двухточечный читатель F0 (RD1), двусторонность после экстремума (TR1), отношение
ночи и утра (CW1; клетка 2020–25 не подтвердилась на вскрытом 2026 — [`FREEZE_CW1`](base/105/FREEZE_CW1.md)),
выживание нормы после полудня (SV1), скрытый порядок high/low внутри минуты по тикам 2024–25 (TK1), возврат в
премаркете (RV1). [Атлас выплат](setups/library/PAYOFF_ATLAS.md): в архиве нет устойчивого независимого члена
библиотеки помимо составляющих B1; дневная дверь S-09 в совместном прогоне плюс на поиске и минус на отложенном
куске. [Карта покрытия](setups/library/PAYOFF_MAP.md): где рынок ходит без B1 — ночь и дни внутри нормы после обеда.
Новый физический канал (тики, секунды) — только с конкретным вопросом, который минутное представление решить не
может; TK1 показал, что порядок high/low таким не является.

**Контракт NQ** ([`research/NQ_MANUAL_TRADING_RESEARCH_PLAN.md`](research/NQ_MANUAL_TRADING_RESEARCH_PLAN.md)):
поправки 2026-09-24 — ежедневная сделка, «каждый день ≥ 0» и «лучшие 5 % дней ≤ 20 %» — требования к итоговой
библиотеке, не к отдельному способу.

**RIZ-кандидат один** — [086](base/086-strukturnaya-vilka-close-break-kandidat-bez-perenosa.md)/[091](base/091-ekonomicheskoe-zavershenie-086-ispolnimyy-istoricheskiy-kandidat.md),
сетап [S-16](setups/S-16-prodolzhenie-tolchka-posle-close-break.md), заморожен в
[`FREEZE_091_TEMPORAL`](base/091/FREEZE_091_TEMPORAL.md); проверка во времени 2026-05…07 — UNRESOLVED (три эпизода);
ждёт ленты вместе с B1.

**Что известно об объекте RIZ.** Короткая картина — в [`reference/SCENE.md`](reference/SCENE.md), точные слова —
в [`GLOSSARY.md`](GLOSSARY.md): T0 — поздняя точка отобранной биографии тонкого гэпа; первое касание (ретест) у
60 % сцен на следующей минуте — это размер свечи; обе границы достигаются как обычная линия на том же расстоянии.

**Открытые вопросы уровня задачи — ориентиры, не назначение задачи:**

- правило признания B1 вперёд заморожено 2026-09-24 ([`FREEZE_B1` §5](setups/library/FREEZE_B1.md)): 125 и 250 слепых сессий — обзоры, 500 — решение; ждёт ленты;
- какое наблюдение заставило бы отказаться от минутных цены и времени как достаточного представления;
- если B1 — плата за постоянную экспозицию к редким долгим состояниям, какой другой независимый риск может платить
  выплатой обратной формы (частые мелкие плюсы).

<details>
<summary>След предыдущего прохода: локальные проверки 059 от 2026-09-07</summary>

Эта запись сохраняет прежнее наблюдение и адрес расчёта; новой проверки при
редактуре входа не проводилось. Межиндексное рассогласование из переданной
заметки на префиксе не воспроизводится: на закрытых минутах соседа разница по
эпохам +0,072 / +0,018 / −0,008 свечи вместо обещанных −0,433 — эффект жил на
окне ±2, то есть на ещё не закрывшихся минутах соседа. Первый контакт с выходной
границей не отличается от обычной минуты с тем же уже случившимся ходом: знак
пляшет по эпохам ±0,3 свечи, и различие будущих исходов не накапливается до
касания (0,03–0,16 свечи за три минуты до него). Расчёты — `work/059/`.
Это локальные материалы прежней работы, в свежем клоне их нет.

</details>

## Локальная среда

ES, NQ, YM; каждый целый ТФ 1–1440; лента 2006-01-05 … 2026-05-04.
Поле построено, заморожено и открыто только на чтение.

```powershell
python -B -m g3riz.cli status
python -B -m pytest tests -q -p no:cacheprovider
```

Статус должен видеть 4 320 ячеек. Живая перепись главнее снимка
`PROJECT_STATE.json`. Свежий клон больших локальных массивов не содержит:
без подключённых `data/market` и `data/field` статус их не увидит. Это
ограничение доступа, а не потеря данных, и пересборку оно не разрешает.

Подключение — существующие каталоги `data/market/<инструмент>/` и
`data/field/<инструмент>/` внутри корня клона: обычные папки либо ссылки на
имеющуюся локальную копию. Проверяй `corpus_id` по `SOURCE_DATA.json` и
манифестам; число ячеек само по себе не удостоверяет происхождение байтов.
Если нужных массивов на машине нет, документацию и код можно изучать сразу;
для расчёта требуется местоположение готовой копии с установленным происхождением.

Среда общего слоя — `pip install -e ".[dev]"`, то есть numpy/pandas/pyarrow и
pytest из `pyproject.toml`; ничего сверх этого он не требует. Зависимости
конкретного расчёта живут рядом с ним: `setups/S-05/requirements.txt` добавляет
`numba`, `matplotlib` и `exchange_calendars`, нужные S-04…S-07, а не проекту.

[`.github/workflows/tests.yml`](.github/workflows/tests.yml) гоняет только этот
общий слой. Поле в CI не загружается и не материализуется; всё помеченное
`needs_field` там пропускается, и пропуск означает «не смотрели», а не «пусто».
Предметные проверки кандидатов CI не запускает и покрытием исследований не
притворяется: `python -B -m pytest setups/S-07/test_execution.py`.

Менять идентичности, пересобирать поле или возвращать materializer можно только
по отдельному решению трейдера: новое поколение семантики либо восстановление
после подтверждённой потери. Materializer лежит в истории git.

## Чем читать

Быстрое чтение нескольких паспортов, без изменения поля:

```powershell
python -B -m g3riz.cli query --instrument NQ --tf 54 --limit 3
```

Это адреса для чтения сцены: полный паспорт содержит поздние сведения и
не является готовым префиксом решения.

`Field.passports`, `events`, `film`, `films`, `minute_windows`, `objects_at`.
Рабочий пример и сборка сцены — в [`reference/`](reference/README.md).

`Film` имеет один T0, отрицательный pre-roll и явно названный конец: удаление,
конец Blue, край архива, бюджет наблюдения или `end_position` исследования.
`film.truncate(p)` показывает то, что было доступно к минуте `p`.

`core_tracks` — механические чтения. Выбор порога, окна или задержки — уже
именованная исследовательская линза; переиспользуемые живут в
`src/g3riz/lenses/`. Изменение смысла линзы получает новую версию, а не тихую
правку старой. Треки и линзы считают; статус знанию присваивает исследователь.

## Где что лежит

| Что | Куда |
|---|---|
| Вопрос и ответ о рынке | `base/NNN-*.md`, одна пропозиция — один номер |
| Указатель на текущее место | «Сейчас на фронте» в `base/README.md` |
| Точные значения слов | `GLOSSARY.md` |
| Объект в свечах, учебные случаи | `reference/SCENE.md` |
| Переход от сцены к вопросу и тесту | `reference/CONTRACT.md` |
| Проверка самого входа | `reference/ENTRY_CHECK.md` |
| Карта модулей чтения поля | `src/g3riz/README.md` |
| Происхождение машины, Pine | `reference/README.md`, `reference/pine/` |
| Гипотеза действия | `setups/` |
| Сохранённый расчёт значимого результата | `base/NNN/` или `setups/S-NN/`, рядом с карточкой |
| Черновые прогоны, логи, тяжёлые результаты | `work/NNN/`, `data/research/NNN/` |
| Страховка общего слоя | `tests/` |

`work/`, `data/` и большие массивы в git не попадают. Нужные для повторения код,
параметры, команда и происхождение входов сохраняются вместе с карточкой.
Общий слой чтения не задаёт длительность, отбор или правила сделки: это код
конкретного кандидата. Не добавляй универсальный движок без реальной потребности.

## Проверка входа

```powershell
python -B -m g3riz.cli entry-check make --version 2 --paper work/NNN/paper.md --key <вне репозитория>
python -B -m g3riz.cli entry-check score --key <ключ> --answers work/NNN/answers.json
```

Пять пар анонимизированных сцен и задача о раннем состоянии до будущего
паттерна. Сверяются ответы и выбранные исследовательские решения; коротких
полей недостаточно. Версия 1 оставлена для старых прогонов. Что измерено — в
[`reference/ENTRY_CHECK.md`](reference/ENTRY_CHECK.md).
