# Станок

Изменяемое состояние проекта: что сейчас лежит на столе, чем это читать и куда
что класть. Правила работы и мандат — в [`AGENTS.md`](AGENTS.md); этот файл
устаревает быстрее и меняется без пересмотра тех правил.

## Поле

ES, NQ, YM; каждый целый ТФ 1–1440; лента 2006-01-05 … 2026-05-04.
Поле построено, заморожено и открыто только на чтение.

```powershell
python -B -m g3riz.cli status
python -B -m pytest tests -q -p no:cacheprovider
```

Статус должен видеть 4 320 ячеек. Живая перепись главнее снимка
`PROJECT_STATE.json`. Свежий клон больших локальных массивов не содержит:
без подключённых `data/market` и `data/field` статус их не увидит. Это
ограничение доступа, а не потеря данных, и пересборку оно не разрешает.

Менять идентичности, пересобирать поле или возвращать materializer можно только
по отдельному решению трейдера: новое поколение семантики либо восстановление
после подтверждённой потери. Materializer лежит в истории git.

## Чем читать

`Field.passports`, `events`, `film`, `films`, `minute_windows`, `objects_at`.
Рабочий пример и сборка сцены — в [`reference/`](reference/README.md).

`Film` имеет один T0, отрицательный pre-roll и явно названный конец: удаление,
конец Blue, край архива, бюджет наблюдения или `end_position` исследования.
`film.truncate(p)` показывает то, что было доступно к минуте `p`.

`core_tracks` — механические чтения. Выбор порога, окна или задержки — уже
именованная исследовательская линза; переиспользуемые живут в
`src/g3riz/lenses/`. Изменение смысла линзы получает новую версию, а не тихую
правку старой. Треки и линзы считают; статус знанию присваивает исследователь.

## Где что лежит

| Что | Куда |
|---|---|
| Вопрос и ответ о рынке | `base/NNN-*.md`, одна пропозиция — один номер |
| Указатель на текущее место | «Сейчас на фронте» в `base/README.md` |
| Точные значения слов | `GLOSSARY.md` |
| Объект в свечах, учебные случаи | `reference/SCENE.md` |
| Проверка самого входа | `reference/ENTRY_CHECK.md` |
| Происхождение машины, Pine | `reference/README.md`, `reference/pine/` |
| Гипотеза действия | `setups/` |
| Сохранённый расчёт значимого результата | `base/NNN/` или `setups/S-NN/`, рядом с карточкой |
| Черновые прогоны, логи, тяжёлые результаты | `work/NNN/`, `data/research/NNN/` |
| Страховка общего слоя | `tests/` |

`work/`, `data/` и большие массивы в git не попадают. Нужные для повторения код,
параметры, команда и происхождение входов сохраняются вместе с карточкой.
Общий слой чтения не задаёт длительность, отбор или правила сделки: это код
конкретного кандидата. Не добавляй универсальный движок без реальной потребности.

## Текущий этап

Уточнение трейдера 2026-09-06: сначала искать сетапы на существующей истории
и проверять их на ней же. Новые RIZ и новые данные сейчас не нужны.
`Field` читает готовое поле; отдельный загрузчик или возврат materializer
не являются условием начала и завершения этого этапа.

Проверка здесь означает повторяемость узнавания без будущих свечей, корректное
исполнение, деньги после расходов и устойчивость результата на доступной
истории. Исторический результат сохраняется в библиотеке с честно названным
поиском и ограничениями. Повтор победителя дополняется оценкой способа выбора
в пределах восстановимой истории; способы и границы — в
[`setups/README.md`](setups/README.md#как-перепроверять-на-готовой-истории).
Расчёт такой оценки для прежних кандидатов ещё не выполнен этим изменением
порядка. Общий статистический модуль не добавлен.
2026-09-07 завершён новый проход от свечных сцен до исполнения и оценки выбора:
[S-04](setups/S-04-vozvrat-vstrechnymi-svechami.md) отрицателен;
[S-05 v2](setups/S-05-otkat-vstrechnymi-svechami.md) даёт частое событие
(96,3% известных дней NQ с 2020 года) и исторический плюс, но устойчивое
прибыльное преимущество не установлено. Оценка выбора по всему сохранённому
семейству 432 правил дала −$612,50. Код, все варианты, проверка свечей и
повторение лежат рядом с карточками; вход — [S-05/README.md](setups/S-05/README.md).
Старые расчёты не перенесены автоматически из `work/` к карточкам.
Продолжение 2026-09-07 — [S-06](setups/S-06-svechnaya-pauza-u-riz.md):
пробой свечной паузы у существующего RIZ и возврат после одностороннего прокола.
Оба действия отклонены; затем определение паузы сужено до закрытий внутри общей
области (v3) и оба действия повторены на нём. Все 144 правила отрицательны
после расходов, лучшее — −$25 на 847 сделках. Узнавание на NQ упало с 245 804
до 52 669 минут, средний убыток на сделку не изменился. Годовой выбор внутри
S-06 впервые вышел в рынок и дал −$22 950; совместный с S-04/S-05 остался
−$612,50. Общее сохранённое семейство — 576 правил. Повторение, проверка v3
и свечи — [S-06/README.md](setups/S-06/README.md).
2026-09-07, продолжение: [S-07](setups/S-07-drayf-pervyh-minut-sessii.md).
Проверены две линии. Межиндексное рассогласование из переданной заметки на
префиксе не воспроизводится: на закрытых минутах соседа разница по эпохам
+0,072 / +0,018 / −0,008 свечи вместо обещанных −0,433 — эффект жил на окне ±2,
то есть на ещё не закрывшихся минутах соседа. Первый контакт с выходной границей
не отличается от обычной минуты с тем же уже случившимся ходом: знак пляшет по
эпохам ±0,3 свечи, и различие будущих исходов не накапливается до касания
(0,03–0,16 свечи за три минуты до него). Расчёты — `work/059/`.
Живой результат — S-07: сторона по предрыночному положению цены, решение на
3-й минуте сессии, удержание 60–120 минут. NQ 2020–2026 +$273 050 на 1 563
сделках, 7 прибыльных лет из 7, медиана +$115; годовой выбор среди всех 270
конфигураций даёт +$204 150, то есть результат не создан выбором задним числом.
Ограничения: весь итог в правом хвосте (без лучших 5% дней минус), рабочего
стопа нет, ES около нуля, YM отрицателен, поле RIZ фильтром не работает.
Ведение и риск посчитаны: единственное окупающееся вмешательство — выход на
15-й минуте при минусе (доход/просадка 7,41 против 6,21 у простого удержания);
безубыток и трейлинг съедают от половины до двух третей результата;
катастрофический лимит 16 свечей стоит 1,8% итога и втрое обрезает худший день;
размер считается в свечах — ⌊риск / (16 × стоимость свечи)⌋.
Версия v1 заморожена 2026-09-07 с хешами кода, входов и среды
(`setups/S-07/FROZEN_v1.json`); правило перенесено в Pine для живой проверки.
Дальше по этой линии полезны только портфельный расчёт с прежними кандидатами,
микроконтракт и данные, которых правило не видело, — не новые пороги.
Независимое подтверждение — отдельное свойство,
которого повтор того же архива не создаёт; сейчас его отсутствие не блокирует
ни поиск, ни историческую проверку, ни представление кандидата трейдеру.

## Проверка входа

```powershell
python -B -m g3riz.cli entry-check make --paper work/NNN/paper.md --key <вне репозитория>
python -B -m g3riz.cli entry-check score --key <ключ> --answers work/NNN/answers.json
```

Пять пар анонимизированных сцен, по одной оси возможного неверного чтения на
пару. Что измерено и с какими границами — в
[`reference/ENTRY_CHECK.md`](reference/ENTRY_CHECK.md).
