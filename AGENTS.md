# G3 cold-entry instruction

Read `README.md` and `G3_RIZ_FIELD_IMPLEMENTATION_BRIEF.md`, then run
`python -m g3riz.cli status` with `PYTHONPATH=src`. Treat generated manifests as
the actual materialization state. Resume missing cells with the documented
`build` command; do not rebuild completed cells or infer state from chat history.
G2 and Pine/RIZ repositories are read-only semantic benchmarks, never runtime
dependencies or edit targets.

