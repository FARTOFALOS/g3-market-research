# 090 — граница представления R₀=(dpos,psig): единственный локатор остатка оказался артефактом грубого estimator'а, не рыночным каналом

**Статус: `COMPLETED — located residual was baseline-reading, not additional information; frozen holdout not opened.`**
Не «R₀ достаточно», не «канал опровергнут», не поиск состояния.

- **вывод:** держится в объявленных границах.
  - **Вопрос ветки (узкий).** Несёт ли доступная не позднее курсора `q` величина `C`
    дополнительную информацию о немедленном продолжении `CONT*` сверх объявленного
    представления `R₀=(dpos,psig)` (089) и контекста `K=(side,ord)`. Это **не** тест
    достаточности `R₀` относительно всего remaining path.
  - **K не добавил обнаружимого.** В этом train-анализе `side`/`ord` не дали
    обнаружимой дополнительной predictive information сверх выбранного чтения `R₀`
    (Δlog-loss −0,0008 out-of-fold). Формулировать как «поглощены R₀» нельзя —
    empirical recoverability ≠ semantic equivalence.
  - **Единственный локатор — приграничный угол.** Кросс-fit kNN(k=1000) был
    хорошо откалиброван везде, кроме зоны низкого `dpos` (≈1) и высокого `psig`
    (≈3): `P_obs=0,389` против `P_pred=0,475`, resid −0,159 (n=1454, 1396 сцен).
  - **Локатор растворяется под адекватным чтением того же R₀.** Малый k (kNN k=50)
    даёт 0,391 ≈ наблюдаемому; параметрический logit с interaction и boundary-базой
    `1/(1+dpos)` — 0,379 (resid +0,008); edge-скорректированный local-linear LOESS —
    0,389. Промах k=1000 был чистым недо-изгибом грубого сглаживания. Непрерывная
    поверхность гладкая и монотонная по `psig` при фиксированном `dpos`
    (0,642→0,297), без бугра сверх гладкого чтения. Support реальный (1069 разных
    t0), композиция не скрытая (north 0,387 vs south 0,386; по годам 0,28–0,47).
  - **Итог.** Выжившей остаточной неоднородности, мотивирующей prefix-оператор `C`,
    в единственной локализованной области нет. Кажущийся остаток объясняется более
    сильным чтением `R₀` (interaction/boundary-aware вместо квантильных бинов 089) —
    это уточнение чтения представления, не новый канал. **TEST (2016–2018) не
    открыт — holdout не потрачен.**
- **на чём стоит:** [`FREEZE_090`](090/FREEZE_090.md). Population prefix-honest
  (объект-сцена = тройка `(t0_spine_pos, side, exit_boundary)`, 65 972 сцены;
  канонизация только по префиксу; **представитель по будущей `life` из 089
  отменён**). `CONT*` с явной цензурой (`gap`/`archive`/`freshness_lost` → censored,
  не 0). Time-blocked split 2016-01-01 с purge/embargo 7д, scene-disjoint. Код —
  `base/090/{pop,split,xray1,xray2,adequacy}.py`. `Volume` не загружался.
- **чем подтверждено:** `нет`. Это train-only диагностика на уже экспонированной NQ
  discovery, не внешняя репликация. TEST оставлен запечатанным (не открывался), так
  как выжившего кандидата `C` для заморозки и проверки не возникло.
- **честно о границе:** это **не** доказательство полной достаточности `R₀`.
  Установлено: (а) единственный локатор, который дала эта ветка, — артефакт грубого
  estimator'а; (б) вне угла калибровка и так была в пределах ±0,01; (в) `no
  miscalibration ≠ no additional information` — sufficiency не проверялась, C-канал
  не искался широким перебором (запрещено мандатом).
- **относительно объекта:** Film-1 наружная сцена одного `riz_id` (088/089),
  курсор — живой фронт-событийный бар; 162 782 курсора, 25 263 сцены с ≥1 событием,
  цензура 105 (0,06%).
- **территория:** только NQ discovery 2006-01-05 … 2018-12-24 (train 2006–2015 после
  split, test 2016–2018 запечатан).
- **откуда вопрос:** мандат куратора (2026-09-17): проверить, где перестаёт хватать
  уже существующего представления состояния 089; условие 089 «ломается когда» —
  найти при сопоставимых `dpos/psig` воспроизводимую структуру `CONT` вдоль оси, не
  сводимой к положению/масштабу.
- **что видело идею:** discovery-фильмы видны на train как рентген; model residual
  использован только как локатор X-ray, не как доказанный market residual.
- **попытка:** 1
- **работа локально:** `work/090/{cursors,scenes,cursors_split,train_xray}.parquet`;
  код — `base/090/`.
- **связано:** продолжает [089](089-prodolzhenie-grammatiki-menyaetsya-nepreryvno-s-polozheniem.md)
  (тестирует названное там «ломается когда»); опирается на объект
  [088](088-joint-faza-net-nablyudaemoy-granitsy-protsessa.md); методологический
  урок вынесен в [`RESEARCH_GUARDRAILS`](../reference/RESEARCH_GUARDRAILS.md)
  (model residual как локатор; calibration ≠ sufficiency; future-dependent
  representative selection).

## Чего это не устанавливает
- Полной достаточности `R₀` (sufficiency-тест не проводился; C-канал широким
  перебором не искался).
- Отсутствия дополнительной информации где-либо вне единственного проверенного
  локатора.
- Что `side`/`ord` семантически сводимы к `R₀` (показано лишь отсутствие
  обнаружимой добавки в этом анализе).
- Переноса на ES/YM, evaluation, торгуемости.
